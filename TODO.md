# TODO

## P0 — вернуть прежнюю разметку DEVKIT1 (наивысший приоритет)

Решение от 17 сентября 2026 года: выполнить первым, до следующего релиза для DEVKIT1.
Вернуть разметку классической ESP32 4 МБ к состоянию до коммита `9ef176c`, сохранив
совместимость с устройствами, на которые уже записали увеличенные разделы. Возврат
разметки пока не выполнен; ниже — условия реализации и проверки.

- [ ] В `partitions_4mb.csv` восстановить значения из `partitions.csv` в `9ef176c^`:

  | Раздел | Смещение | Размер |
  |---|---|---|
  | `nvs` | `0x9000` | `0x5000` (20 КиБ) |
  | `otadata` | `0xE000` | `0x2000` (8 КиБ) |
  | `ota_0` | `0x10000` | `0x1C0000` (1,75 МиБ) |
  | `ota_1` | `0x1D0000` | `0x1C0000` (1,75 МиБ) |
  | `spiffs` / LittleFS | `0x390000` | `0x70000` (448 КиБ) |

- [ ] Применить к `esp32`, `esp32-mock` и наследующему `esp32-hook`. Сохранить отдельные
  таблицы плат: `partitions_16mb.csv` и разметку ESP32-S3 не менять. Не откатывать весь
  коммит `9ef176c` целиком.
- [ ] Проверять размер образа DEVKIT1 в CI по пределу старого слота: не более
  `0x1C0000` / 1 835 008 байт. После интеграции BLE повторно проверить фактический размер,
  не заменять его оценкой через сумму двух отдельных бинарников.
- [ ] Поддержать одну прошивку на обеих уже установленных разметках DEVKIT1:
  исходной (слоты 1,75 МиБ, LittleFS 448 КиБ) и увеличенной (1,875 МиБ, 192 КиБ).
  Получать адреса и размеры разделов из таблицы самого устройства; не требовать записи
  старого `partitions.bin` на работающие устройства с увеличенной разметкой.
- [ ] Перед выпуском проверить на обеих разметках обновление одного и того же
  `firmware.bin` по Wi-Fi в оба OTA-слота, повторную загрузку и сохранность настроек и
  журналов. Проверить работу при меньшем файловом разделе 192 КиБ и совместимый откат
  между образами без смены установленной таблицы.
- [ ] Согласовать USB-комплект и сценарий Kanvex Service Tool: до записи сравнивать
  установленную и целевую разметки. Не подменять таблицу существующего устройства
  молча. При различии таблиц останавливать обычную прошивку: смена разметки требует
  отдельного подтверждаемого сценария с резервированием/восстановлением данных,
  корректным выбором загрузочного слота и проверкой запуска. Сейчас обычная
  USB-прошивка Tool такую миграцию не выполняет.
- [ ] Синхронизировать `docs/flash-layout.md`, `docs/technical-details.md`, инструкции
  первой прошивки/обновления, CI и релизные USB-артефакты. Описать обычное обновление по
  Wi-Fi без обязательной миграции уже работающих DEVKIT1 и отдельно — USB-восстановление.

## Проверка OBD-II на автомобиле

- Проверить обнаруженный набор поддерживаемых PID и темп ответов на реальном автомобиле
- Убедиться, что фоновый OBD-II-опрос не ухудшает частоту и надежность основного UDS-опроса
