#pragma once

// Версия прошивки — единственное место в проекте, где она задается.
//
// Формат YYYY.M.Z: год, мажорная версия, минорная (описание в README).
// Строка показывается внизу дисплея, отдается в GET /version и GET /metrics,
// а веб-интерфейс сравнивает ее с последним релизом на GitHub.
//
// Макрос, а не constexpr: препроцессор склеивает версию с окружением и датой
// сборки в маркер образа (FW_IMAGE_TAG в src/OtaImage.cpp), по которому
// читается версия прошивки из соседнего OTA-слота.
//
// CI сверяет это значение с именем тега: .github/workflows/build.yml грепает
// строку `#define FW_VERSION` именно из этого файла, поэтому переименование
// макроса или переезд в другой файл требует правки workflow
#define FW_VERSION "2026.3.4"
